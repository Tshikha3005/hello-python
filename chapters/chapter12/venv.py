# pip3 install virtualenv
# virtualenv env(this env is a folder name you can give any name instead of env)
# An env which is same as the system interpreter but is isolated from the other Python env on the systems.
# source env/bin/activate to activate venv
# pip freeze > requirements.txt to create requirements txt file